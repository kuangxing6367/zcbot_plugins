# llm_plugin_gen 插件元信息与依赖声明
name: LLM 开发助手
version: 1.5.0
author: ZGRIC
description: 对话式 LLM 插件开发（Claude Code 风格：会话编号 #00~#99 + ask_user 批准门控 + 自动压缩 + /ai stop 暂停，全异步）
priority: 100

# Python 依赖（框架启动时自动检查并安装缺失项）
dependencies:
  python:
    - "httpx>=0.27.0"
    - "requests>=2.28.0"
    - "pyyaml>=6.0"

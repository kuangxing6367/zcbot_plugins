# llm_plugin_gen 插件元信息与依赖声明
name: LLM 开发助手
version: 1.2.0
author: ZGRIC
description: 通过 OpenAI 兼容接口让 LLM 编写插件并管理插件文件（支持人格/skills/函数调用）
priority: 100

# Python 依赖（框架启动时自动检查并安装缺失项）
dependencies:
  python:
    - "requests>=2.28.0"
    - "pyyaml>=6.0"

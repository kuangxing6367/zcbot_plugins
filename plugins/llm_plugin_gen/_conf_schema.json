{
  "base_url": {
    "type": "string",
    "description": "OpenAI 兼容接口地址",
    "default": "https://api.openai.com/v1",
    "hint": "如 https://api.openai.com/v1，或本地 vllm/ollama: http://127.0.0.1:8000/v1"
  },
  "api_key": {
    "type": "string",
    "description": "API Key",
    "default": "",
    "hint": "本地模型可留空"
  },
  "model": {
    "type": "string",
    "description": "模型名",
    "default": "gpt-4o-mini",
    "hint": "如 gpt-4o-mini / deepseek-chat / qwen2.5"
  },
  "temperature": {
    "type": "number",
    "description": "采样温度（越低越稳定）",
    "default": 0.3
  },
  "max_tokens": {
    "type": "number",
    "description": "最大生成 token 数",
    "default": 8192
  },
  "max_tool_rounds": {
    "type": "number",
    "description": "函数调用最大轮数（0 表示不限制，默认不限制）",
    "default": 0
  },
  "session_max_msgs": {
    "type": "number",
    "description": "会话历史最多保留消息条数（超 2 倍自动触发压缩）",
    "default": 30
  },
  "session_max_chars": {
    "type": "number",
    "description": "会话总字符超过 2 倍此阈值时自动压缩（旧消息 LLM 摘要）",
    "default": 8000
  },
  "persona": {
    "type": "string",
    "description": "人格设定（自定义 system prompt，可写中文）",
    "default": "你是 ZCBOT OneBot QQ 机器人框架的插件开发专家代理（AI 项目助手），擅长把需求变成可运行、健壮的 Python 插件代码，严格遵循 AI 项目助手工作流规范 V1.0（四阶段闭环）。",
    "hint": "留空使用默认人格"
  },
  "skills": {
    "type": "string",
    "description": "技能列表（每行一条，注入 system prompt）",
    "default": "生成插件：根据用户需求生成符合框架规范的插件代码\n修改插件：阅读现有插件代码后修复 Bug / 增加功能\n文件操作：使用 ls/read/search/write/edit/append/mkdir/rm/rename 管理插件目录文件\n插件管理：load_plugin / unload_plugin / reload_plugin / list_plugins",
    "hint": "每行一个技能，换行分隔"
  },
  "cwd": {
    "type": "string",
    "description": "LLM 文件操作的工作目录",
    "default": "",
    "hint": "留空默认项目根目录"
  }
}
